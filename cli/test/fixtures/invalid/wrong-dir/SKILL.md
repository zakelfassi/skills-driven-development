---
name: not-wrong-dir
description: A skill whose name does not match its directory. Use when testing that name/dir mismatches are caught.
---

# Wrong Dir

The directory is `wrong-dir` but `name` says `not-wrong-dir`. This should fail validation.
